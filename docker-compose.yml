name: zenobase-web
services:
  nginx:
    container_name: nginx
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:9000
    ports:
      - "8080:80"
